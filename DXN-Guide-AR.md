# دليل DXN — رفع الموقع وبناء تطبيق الأندرويد

مرحباً 👋 هذا الدليل مكتوب خطوة بخطوة. لا تحتاج خبرة برمجية.

---

## ما الموجود في الملف المضغوط؟

| المجلد | الوصف |
|---|---|
| `build` | موقعك جاهز للنشر (هذا الذي ترفعه على GitHub Pages) |
| `android` | مشروع تطبيق الأندرويد الجاهز لبناء ملف APK |
| `.github/workflows` | ملف يجعل GitHub يبني لك التطبيق مجاناً دون تثبيت أي برنامج |

---

# القسم الأول: رفع الموقع على GitHub

### 1) أنشئ مستودعاً جديداً
1. ادخل على github.com وسجل الدخول.
2. اضغط على علامة **+** في الأعلى ← **New repository**.
3. الاسم: مثلاً `dxn-app`.
4. اختر **Public** (مطلوب للنشر المجاني).
5. اضغط **Create repository**.

### 2) ارفع ملفات الموقع
1. في المستودع الجديد اضغط **Add file ← Upload files**.
2. افتح مجلد `build` عندك ، واسحب **محتوياته** (ليس المجلد نفسه) إلى الصفحة:
   - `index.html`
   - `fonts.css`
   - `manifest.json`
   - `sw.js`
   - مجلد `img`
   - مجلد `icons`
3. انتظر حتى يكتمل الرفع (الصور كثيرة، قد يأخذ دقيقة).
4. اكتب في خانة التعليق: `النسخة الأولى` ثم اضغط **Commit changes**.

> ملاحظة: الملف `index.html` يجب أن يكون في الجذر (أول صفحة المستودع)، وليس داخل مجلد `build`.

### 3) فعّل النشر
1. اضغط تبويب **Settings** ثم من القائمة الجانبية **Pages**.
2. في **Source** اختر `Deploy from a branch`.
3. في **Branch** اختر `main` والمجلد `/ (root)` ثم **Save**.
4. انتظر دقيقتين، ثم حدِّث الصفحة — سيظهر لك رابط الموقع في الأعلى.

✅ الموقع الآن يعمل، ويمكن تثبيته على الجوال من قائمة المتصفح ← “إضافة إلى الشاشة الرئيسية”.

---

# القسم الثاني: الحصول على ملف التطبيق (APK)

أمامك طريقتان. **الأولى أسهل ولا تحتاج تحميل أي برنامج.**

## الطريقة الأولى (الموصى بها): GitHub يبني لك التطبيق

### 1) أنشئ مستودعاً ثانياً للتطبيق
اسمه مثلاً `dxn-android`. يمكن أن يكون Private أو Public.

### 2) ارفع مجلدي `android` و `.github`
1. **Add file ← Upload files**.
2. اسحب مجلد `android` كاملاً ومجلد `.github` كاملاً.
3. اضغط **Commit changes**.

> إن لم يقبل المتصفح سحب مجلد `.github` (لأنه يبدأ بنقطة)، أنشئه يدوياً:
> **Add file ← Create new file**، واكتب في خانة الاسم بالضبط:
> `.github/workflows/build-apk.yml`
> ثم افتح الملف عندك بالمفكرة، وانسخ محتواه والصقه، ثم **Commit**.

### 3) شغّل البناء
1. ادخل تبويب **Actions** في المستودع.
2. إن طلب منك تفعيل الميزة، اضغط **I understand my workflows, go ahead and enable them**.
3. اختر **Build APK** من اليمين ثم **Run workflow ← Run workflow**.
4. انتظر حوالي 5–10 دقائق حتى ترى علامة ✅ خضراء.

### 4) حمّل التطبيق
1. اضغط على اسم التشغيل الناجح.
2. في أسفل الصفحة قسم **Artifacts** ← اضغط `DXN-app`.
3. سينزل ملف مضغوط داخله ملف APK — هذا هو تطبيقك.
4. انقله للجوال وافتحه، واسمح بالتثبيت من مصدر غير معروف عند الطلب.

## الطريقة الثانية: Android Studio على جهازك
1. حمّل وثبّت **Android Studio** من موقعه الرسمي.
2. افتحه ← **Open** ← اختر مجلد `android` الموجود عندك.
3. انتظر حتى ينتهي من تحميل الملفات المطلوبة (أول مرة تأخذ وقتاً).
4. من القائمة العليا: **Build ← Build Bundle(s)/APK(s) ← Build APK(s)**.
5. عند الانتهاء اضغط **locate** في الإشعار لفتح مكان ملف APK.

---

# تحديث التطبيق لاحقاً

عند تعديل أي شيء في الموقع:
1. ارفع الملفات الجديدة لمستودع الموقع (القسم الأول).
2. وارفع نفس الملفات بداخل مستودع التطبيق في المسار:
   `android/app/src/main/assets/www/`
3. ارفع رقم الإصدار داخل الملف `android/app/build.gradle`:
   - `versionCode 1` ← `2`
   - `versionName "1.0"` ← `"1.1"`
4. شغّل البناء مرة أخرى من تبويب Actions.

---

# أسرار لوحة المالك (خاصة بك وحدك)

لوحة الأعضاء والتنبيهات ورسالة الترحيب **لا تظهر للزوار أبداً**. لفتحها:
- اضغط **5 مرات متتالية** على سطر السنة في أسفل الصفحة، ثم أدخل الرمز السري.
- أو أضف `?owner=1` في نهاية رابط الموقع.

لتغيير الرمز السري: افتح `index.html` بالمفكرة ، ابحث عن `OWNER_CODE` وغير الرقم بين العلامتين.

---

# ملاحظات مهمة وصريحة

- ملف APK الناتج موقّع بمفتاح تجريبي، وهو ممتاز للتوزيع المباشر على الموزعين عبر واتساب أو رابط تحميل. لرفعه على Google Play لاحقاً تحتاج مفتاح توقيع خاص بك وحساب مطور (25 دولار لمرة واحدة) — نعملها عندما تريد.
- التطبيق يعمل بدون إنترنت، وكل البيانات تُحفظ على الجوال نفسه فقط.
- لم أستطع تجربة الموقع داخل متصفح حقيقي أو بناء APK فعلياً في بيئتي، لذلك جرّب التطبيق على جوالك وأخبرني بأي ملاحظة لأصلحها.
